<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/rlBackground"
    xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorDark_semitrans">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/padNormal"
        android:background="@color/colorLight"
        android:orientation="vertical">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Voeg een nieuwe Kleuter toe."
            android:textColor="@color/colorDark"
            android:textSize="@dimen/textNormal"
            android:textStyle="bold"
            android:layout_marginBottom="@dimen/padSmaller" />

        <EditText
            android:id="@+id/etName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Voornaam"
            android:backgroundTint="@color/colorDark"
            android:textColor="@color/colorDark"
            android:inputType="" />

        <EditText
            android:id="@+id/etFamname"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Achternaam"
            android:backgroundTint="@color/colorDark"
            android:textColor="@color/colorDark"
            android:layout_marginTop="@dimen/padSmall"
            android:inputType="" />
    </LinearLayout>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAddToddlerAccept"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_round_check"
        android:tint="@color/colorAccent"
        android:backgroundTint="@color/colorLight"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_margin="@dimen/padNormal"
        android:visibility="gone" />

</RelativeLayout>